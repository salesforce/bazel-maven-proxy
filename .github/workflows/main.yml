name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run Tests
      uses: ngalaiko/bazel-action@2.0.0
      with:
        args: test //...
